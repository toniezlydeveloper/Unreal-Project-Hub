<UserControl x:Class="UnrealProjectHub.Controls.ProjectRowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

        <Border Padding="12"
                CornerRadius="6"
                Cursor="Hand"
                MouseLeftButtonUp="RaiseClickCallback">

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#252526"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2D2D30"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding Name}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#E6E6E6"/>

                <TextBlock Grid.Row="1"
                           Text="{Binding UProjectPath}"
                           FontSize="11"
                           Foreground="#888"
                           Margin="0,4,0,0"/>
            </Grid>
        </Border>
</UserControl>